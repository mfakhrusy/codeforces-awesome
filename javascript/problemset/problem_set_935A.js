/* jshint -W097 */ // strict -> false (need to have function form of strict mode)
// Fafa and his company
// n -> number of employees
// l -> number of leaders
// each team leader should have same number of employees
// team leader isn't another team leader's employee
// each employee that isn't team leader has to be under exactly one employee

'use strict';

// var numbers = readline().split(" ").map(function(x) { return parseInt(x); });
let a = readline();
console.log(a);